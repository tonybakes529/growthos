# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=          # server only: webhooks + cron worker

NEXT_PUBLIC_APP_URL=http://localhost:3000

# Stripe (optional until payments go live)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Cron / worker auth (Vercel Cron sends "Authorization: Bearer $CRON_SECRET")
CRON_SECRET=

# Transactional email
RESEND_API_KEY=
EMAIL_FROM="Growth OS <team@example.com>"
